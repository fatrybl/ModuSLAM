What should be done for DataManager v 0.0.1:
    1) Description for all methods/classes.
    2) Update drawio diagrams.
    3) Test cases when number of used_sensors < all_sensors.

What should be done for DataManager:
1) Tests :
    a. Integration tests (different regimes: stream -> time_limit -> stream etc.)



What should be done for FrontendManager:
 1) Update drawio diagrams.
 2) Add tests

 What should be done for the Framework:
    1) Rewrite Type(s) s.t. MyPy wont blame and use TypeVar
    2) Update Sphinx docs
    3) Add slots=True to dataclasses and use multiple inheritance where its possible.